# Frontend — Meridian Analytics Agent

Next.js (App Router, TypeScript, Tailwind) UI for the Ask flow and Data
Sources management. See the top-level README for full setup.

```bash
npm install
cp .env.local.example .env.local   # fill in demo tenant/user ids
npm run dev
```
